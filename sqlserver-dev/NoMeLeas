# La metodología es ejecutar el sevidor con up,
# copias la linea del servidor
# luego entrar al contenedor con el alias de sqlserver
# entras al contenedor como root, y usas la la linea copiada para entrar al servidor

#como tarea sería hacer un bashrc con un alias dentro del contenedor para que no haga falta
#copiar la línea antes de entrar

/opt/mssql-tools18/bin/sqlcmd -S localhost -U SA -P "YourStrong@Passw0rd123" -C
